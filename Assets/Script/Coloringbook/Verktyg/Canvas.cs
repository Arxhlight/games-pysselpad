using UnityEngine;

public class Canvas : MonoBehaviour
{
    [SerializeField] private ScriptableObject canvas;
    public Texture2D texture;
    public Vector2Int size;
    // TODO make sure all so comes with the correct setting on the sprite. else set it. 
}
